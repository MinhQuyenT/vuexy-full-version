
import { BCol, BRow, BButton, BSidebar, VBToggle, BCard } from "bootstrap-vue";

const bootStrapVue = {
    components:{
        BRow,
        BCol,
        BCard,
        BButton,
        BSidebar,
        VBToggle,
    }
}

export default bootStrapVue